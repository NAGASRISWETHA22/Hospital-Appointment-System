spring.application.name=hospital-backend

# Database Configuration (Direct URL to avoid host resolution errors)
spring.datasource.url=jdbc:mysql://mysql-201dc059-hospital-managesment-system.e.aivencloud.com:12929/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration (Matching your JwtUtil.java)
app.jwt.secret=SecretKey007HospitalManagementSystemProject2026
app.jwt.expiration-ms=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Security Fix for Bean errors
spring.main.allow-bean-definition-overriding=true

# Server Port for Render
server.port=${PORT:8080}

# Swagger/OpenAPI Path
springdoc.swagger-ui.path=/swagger-ui.html